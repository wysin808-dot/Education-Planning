# 固定宣传册 PDF 版式审查

生成的 `brentvale-wace-admissions-almanac-2026-27.pdf` 已确认是 4 页 A4，但第 1 页内容仅占页面上半部，留有过大空白。

原因是宣传册各 `section` 虽然使用 `print:break-after-page` 分页，却没有 A4 内容区的最小高度或垂直分布规则；现有全局打印规则仅处理边距、容器和防跨页。

已实施：为宣传册每个打印页增加 `min-height: 271mm` 和纵向 flex 布局；封面图提升到 112mm，封面正文用 `space-between` 将品牌区定位至底部。下一步为重新生成并检查四页 PDF。

## 复检结果（2026-08-19）

- 固定宣传册 PDF 已成功生成 4 页 A4，但第 2–4 页仍有明显留白，说明单纯拉高页面高度不足以形成适合外发的满版宣传册。
- 问题更关键的一处在于动态查询类页面：手机端截图式 PDF 虽可打开，但版式会脱离原生打印稿，不适合作为正式升学面谈材料。
- 后续处理方向已经明确：
  1. **动态报告** 停用截图式 PDF，恢复为浏览器原生打印 / 存为 PDF，并在手机端改成清晰的操作指引；
  2. **宣传册** 继续保留固定文件方案，但需要单独优化页面密度与节奏，再重新生成稳定下载版。
